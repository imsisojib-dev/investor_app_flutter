abstract class Environment {
  static const mock = 'mock';       // dummy data — no API needed
  static const dev  = 'dev';        // real API — dev server
  static const prod = 'prod';       // real API — production
}